//Write a JavaScript function that takes a string as input and returns the length of that string.
function strLength(str){
    return "Length of string " + str + " : " + str.length;
}
console.log(strLength("Hello World"));